{
  "name": "hydra-i18n",
  "version": "0.1.0",
  "author": "somebeaver",
  "private": true,
  "scripts": {
    "test": "tap --reporter=spec --no-coverage"
  },
  "devDependencies": {
    "axios": "^0.21.1",
    "fastify": "^3.11.0",
    "hydra-test-tools": "github:somebeaver/hydra-test-tools",
    "tap": "^14.11.0"
  }
}
